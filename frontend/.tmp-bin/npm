#!/bin/sh
if [ "$1" = "run" ] && [ "$2" = "build" ]; then
  shift 2
  exec ./node_modules/.bin/vite build "$@"
fi
printf 'unsupported npm shim invocation:' >&2
printf ' %s' "$@" >&2
printf '\n' >&2
exit 1
